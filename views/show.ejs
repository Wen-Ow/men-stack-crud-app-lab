<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= planet.name %> Details</title>
  </head>
  <body>
    <h1><%= planet.name %></h1>
    <p><%= planet.description %></p>
    <img src="<%= planet.image %>" alt="<%= planet.name %> image" />
    <br />
    <a href="/planets">Back to Home</a>
  </body>
</html>
